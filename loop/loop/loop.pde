float y = 300;
float x = 300;
float angle;
float dia = 20;

void setup(){
 size(900,900);
 surface.setLocation(987,70);
}

void draw(){
  background(0);
  translate(width/2,height/2);
  rotate(radians(angle/3));
  for(float a=0; a<360; a+=10){
    
    pushMatrix();
    if(angle<360) rotate(radians(a)*sin
    
  }
  
}
